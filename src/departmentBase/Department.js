import React from 'react';

export const Department = () => {
    return (
        <div>
            Workers amount:
            <button>Change data</button>
            <button>Delete</button>
        </div>
    );
};
